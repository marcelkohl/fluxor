/** @deprecated Use AttachmentRepositoryPort via resolvePersistence(). */
export { SqliteAttachmentRepository as AttachmentRepository } from "@/features/persistence/adapters/sqlite/sqlite-attachment.repository";
